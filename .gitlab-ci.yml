image: jangrewe/gitlab-ci-android

stages:
  - lint
  - build
  - release

before_script:
  - export GRADLE_USER_HOME=$(pwd)/.gradle
  - chmod +x ./gradlew

# Kotlin lint check
# Check linting
kotlinLint:
  interruptible: false
  stage: lint
  script:
    - ./gradlew ktlintCheck --continue

# Make Project
assembleDebug:
  interruptible: true
  stage: build
  script:
    - ./gradlew assembleDebug
  artifacts:
    paths:
      - app/build/outputs/debug

# Run master branch assembly for dev release candidate.
devRelease:
  interruptible: false
  stage: release
  script:
    - ./gradlew assembleDevRelease
  artifacts:
    paths:
      - app/build/outputs/dev/release